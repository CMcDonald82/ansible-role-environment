---

- hosts: localhost
  remote_user: root
  become: yes
  vars: 
    environment_vars:
      TEST_VAR: test

  tasks:
    - name: Echo env vars
      command: echo '$TEST_VAR'
      register: command_result

    - name: Log env vars
      debug:
        msg: command result is {{ command_result }}

  roles:
    - role_under_test